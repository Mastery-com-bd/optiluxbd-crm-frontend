import { SummaryData } from "@/types/orders";

type Props = {
    summary: SummaryData,
}
export function Summary({ summary }: Props) {
    console.log(summary);
    return (
        <div>

        </div>
    );
};
